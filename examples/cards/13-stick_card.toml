# Stick chart example: vertical lines from y=0 to each intensity value.

[global]
pivot_chart = "{{CLASS}}"
chart_style = { name = "stick", color = "#1e4f9a", line_width = 1.0, alpha = 0.9 }

filepath = "<CARD_DIR>/../data/overlay_demo/{{CLASS}}/msms_spectrum.json"
