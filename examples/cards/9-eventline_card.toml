# Event line example: marks positions along X; Y values are ignored.

[global]
pivot_chart = "{{CLASS}}"
chart_style = "eventline"

filepath = "<CARD_DIR>/../data/overlay_demo/{{CLASS}}/base_signal.json"
